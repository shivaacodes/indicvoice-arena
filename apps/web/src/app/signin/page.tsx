export default function SignInPage() {
  return (
    <main className="grid min-h-screen place-items-center bg-cream px-6">
      <div className="w-full max-w-md border border-sage/40 bg-white p-8 text-center shadow-[0_12px_30px_rgba(0,0,0,0.06)]">
        <p className="font-mono text-xs uppercase tracking-[0.18em] text-muted">Authentication</p>
        <h1 className="mt-3 font-serif text-5xl italic text-charcoal">IndicVoice Arena</h1>
        <p className="mt-3 text-sm text-muted">Auth is disabled in this build for testing.</p>
      </div>
    </main>
  );
}
